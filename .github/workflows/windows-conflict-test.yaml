name: windows-conflict-test

on:
  workflow_dispatch: {}
  push:
    branches: [dev]
    paths:
      - '.github/workflows/windows-conflict-test.yaml'
      - '.github/scripts/win_conflict_inspect.ps1'

permissions:
  contents: read

jobs:
  decouple-check:
    name: Clean coexistence (current AVEE + FlClashX)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download installers
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          New-Item -ItemType Directory -Force dl\flclashx, dl\avee | Out-Null

          Write-Host "=== FlClashX latest release assets ==="
          gh release view --repo pluralplay/FlClashX --json assets --jq '.assets[].name'
          gh release download --repo pluralplay/FlClashX -p "*amd64*setup*.exe" -D dl\flclashx
          if (-not (Get-ChildItem dl\flclashx -Filter *.exe -EA SilentlyContinue)) {
            gh release download --repo pluralplay/FlClashX -p "*setup*.exe" -D dl\flclashx
          }
          if (-not (Get-ChildItem dl\flclashx -Filter *.exe -EA SilentlyContinue)) {
            gh release download --repo pluralplay/FlClashX -p "*.exe" -D dl\flclashx
          }

          Write-Host "=== avee latest pre-release ==="
          $tag = (gh release list --repo ${{ github.repository }} -L 30 --json tagName,isPrerelease | ConvertFrom-Json | Where-Object { $_.isPrerelease } | Select-Object -First 1).tagName
          Write-Host "latest pre-release tag: $tag"
          gh release view $tag --repo ${{ github.repository }} --json assets --jq '.assets[].name'
          gh release download $tag --repo ${{ github.repository }} -p "*amd64*.exe" -D dl\avee
          if (-not (Get-ChildItem dl\avee -Filter *.exe -EA SilentlyContinue)) {
            gh release download $tag --repo ${{ github.repository }} -p "*setup*.exe" -D dl\avee
          }

          Write-Host "=== downloaded ==="
          Get-ChildItem -Recurse dl | ForEach-Object { Write-Host $_.FullName }

      - name: 0 - baseline (clean runner)
        shell: pwsh
        run: '& ./.github/scripts/win_conflict_inspect.ps1 -Label "0. clean runner"'

      - name: 1 - install FlClashX
        shell: pwsh
        run: |
          $fl = Get-ChildItem dl\flclashx -Filter *.exe -Recurse | Select-Object -First 1
          if (-not $fl) { Write-Host "::error::FlClashX installer not found"; exit 1 }
          Write-Host "Installing $($fl.FullName)"
          $p = Start-Process -FilePath $fl.FullName -ArgumentList '/VERYSILENT','/SUPPRESSMSGBOXES','/NORESTART','/NOICONS','/NOCANCEL' -Wait -PassThru
          Write-Host "FlClashX installer exit=$($p.ExitCode)"
          & ./.github/scripts/win_conflict_inspect.ps1 -Label "1. after FlClashX"

      - name: 2 - install current AVEE
        shell: pwsh
        run: |
          $dw = Get-ChildItem dl\avee -Filter *.exe -Recurse | Select-Object -First 1
          if (-not $dw) { Write-Host "::error::avee installer not found"; exit 1 }
          Write-Host "Installing $($dw.FullName)"
          $p = Start-Process -FilePath $dw.FullName -ArgumentList '/VERYSILENT','/SUPPRESSMSGBOXES','/NORESTART','/NOICONS','/NOCANCEL' -Wait -PassThru
          Write-Host "avee installer exit=$($p.ExitCode)"
          & ./.github/scripts/win_conflict_inspect.ps1 -Label "2. after current AVEE"

      - name: 3 - verdict
        shell: pwsh
        run: |
          $keys = @(
            'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Uninstall',
            'HKLM:\SOFTWARE\WOW6432Node\Microsoft\Windows\CurrentVersion\Uninstall',
            'HKCU:\SOFTWARE\Microsoft\Windows\CurrentVersion\Uninstall'
          )
          $dw = $null
          foreach ($k in $keys) {
            Get-ChildItem $k -ErrorAction SilentlyContinue | ForEach-Object {
              $p = Get-ItemProperty $_.PSPath -ErrorAction SilentlyContinue
              if ($p.DisplayName -match 'avee') { $dw = $p }
            }
          }
          if (-not $dw) { Write-Host "::warning::no AVEE uninstall entry - install may have failed"; exit 1 }
          Write-Host "avee InstallLocation = $($dw.InstallLocation)"
          if ($dw.InstallLocation -match 'FlClashX') {
            Write-Host "::error::CONFLICT - avee installed inside FlClashX folder: $($dw.InstallLocation)"
            exit 1
          }
          Write-Host "OK - avee decoupled (own install folder: $($dw.InstallLocation))"

  flclashx-running-then-avee:
    name: FlClashX running first, then install AVEE (runtime helper-port)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download installers
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          New-Item -ItemType Directory -Force dl\flclashx, dl\avee | Out-Null
          gh release download --repo pluralplay/FlClashX -p "*amd64*setup*.exe" -D dl\flclashx
          if (-not (Get-ChildItem dl\flclashx -Filter *.exe -EA SilentlyContinue)) { gh release download --repo pluralplay/FlClashX -p "*setup*.exe" -D dl\flclashx }
          $tag = (gh release list --repo ${{ github.repository }} -L 30 --json tagName,isPrerelease | ConvertFrom-Json | Where-Object { $_.isPrerelease } | Select-Object -First 1).tagName
          Write-Host "avee pre-release: $tag"
          gh release download $tag --repo ${{ github.repository }} -p "*amd64*.exe" -D dl\avee
          Get-ChildItem -Recurse dl | ForEach-Object { Write-Host $_.FullName }

      - name: Install FlClashX, then bring it ALIVE (helper on 47890 + launch app)
        shell: pwsh
        run: |
          $fl = Get-ChildItem dl\flclashx -Filter *.exe -Recurse | Select-Object -First 1
          Start-Process -FilePath $fl.FullName -ArgumentList '/VERYSILENT','/SUPPRESSMSGBOXES','/NORESTART','/NOICONS','/NOCANCEL' -Wait
          $hexe = 'C:\Program Files\FlClashX\FlClashHelperService.exe'
          if (Test-Path $hexe) {
            try { New-Service -Name FlClashHelperService -BinaryPathName "`"$hexe`"" -StartupType Manual -ErrorAction Stop | Out-Null } catch { Write-Host "create FlClashHelperService: $_" }
            try { Start-Service FlClashHelperService -ErrorAction Stop; Write-Host "FlClashHelperService started" } catch { Write-Host "start FlClashHelperService failed: $_" }
          } else { Write-Host "FlClashX helper exe not found at $hexe" }
          Start-Process -FilePath 'C:\Program Files\FlClashX\FlClashX.exe' -ErrorAction SilentlyContinue
          Start-Sleep -Seconds 6
          & ./.github/scripts/win_conflict_inspect.ps1 -Label "FlClashX alive (helper + app)"
          Write-Host "-- who holds 47890 --"; (netstat -ano | Select-String ':47890') | ForEach-Object { Write-Host "  $_" }

      - name: Install avee WHILE FlClashX runs, then start its helper
        shell: pwsh
        run: |
          $dw = Get-ChildItem dl\avee -Filter *.exe -Recurse | Select-Object -First 1
          $p = Start-Process -FilePath $dw.FullName -ArgumentList '/VERYSILENT','/SUPPRESSMSGBOXES','/NORESTART','/NOICONS','/NOCANCEL' -Wait -PassThru
          Write-Host "avee installer exit=$($p.ExitCode)"
          $hexe = 'C:\Program Files\avee\AveeHelperService.exe'
          if (Test-Path $hexe) {
            try { New-Service -Name AveeHelperService -BinaryPathName "`"$hexe`"" -StartupType Manual -ErrorAction Stop | Out-Null } catch { Write-Host "create AveeHelperService: $_" }
            try { Start-Service AveeHelperService -ErrorAction Stop; Write-Host "AveeHelperService started OK" } catch { Write-Host "start AveeHelperService FAILED: $_" }
          } else { Write-Host "avee helper exe not found at $hexe" }
          Start-Process -FilePath 'C:\Program Files\avee\avee.exe' -ErrorAction SilentlyContinue
          Start-Sleep -Seconds 6
          & ./.github/scripts/win_conflict_inspect.ps1 -Label "after avee install (FlClashX still alive)"
          Write-Host "-- who holds 47890 now --"; (netstat -ano | Select-String ':47890') | ForEach-Object { Write-Host "  $_" }

      - name: Summary (informational - does not fail)
        shell: pwsh
        run: |
          $fl = (Get-Service FlClashHelperService -EA SilentlyContinue).Status
          $dw = (Get-Service AveeHelperService -EA SilentlyContinue).Status
          Write-Host "FlClashHelperService = $fl"
          Write-Host "AveeHelperService = $dw"
          Write-Host "NOTE: pre.4 still uses port 47890 for BOTH helpers (the 47896 fix is unreleased)."
          Write-Host "      So avee's helper is expected to FAIL while FlClashX holds 47890 - that PROVES the runtime port conflict."
          Write-Host "      Install folders stay separate (other job); this job isolates the runtime helper-port clash."
