# Upstream sync policy

`upstream` points to `https://github.com/0x-gg/avee-android.git`. Syncs must preserve upstream history, use a dedicated issue branch, avoid resets or destructive cleanup, and record the exact source commit and any AVEE modifications.
