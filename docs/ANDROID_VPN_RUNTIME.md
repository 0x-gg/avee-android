# Android VPN runtime baseline

The upstream Android app uses `VpnService` integration and native Mihomo libraries. Runtime behavior is not claimed as tested in this Phase 0 report because the Android SDK, emulator, and Flutter toolchain are not yet available locally.
